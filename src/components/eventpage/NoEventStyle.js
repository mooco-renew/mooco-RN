import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  imageContainer: {
    justifyContent: "center",
    alignItems: "center",
    top: 60,
  },
  image: {
    width: 350,
    height: 250,
    borderRadius: 10,
  },

  TitleText: {
    fontSize: 28,
    fontWeight: "900",
    top: 30,
  },
  ContentText: {
    fontSize: 15,
    // color: "#FFFFFF",
    top: 40,
    textAlign: "center",
    lineHeight: 20,
  },
});
